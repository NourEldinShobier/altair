{
  "name": "altair",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun test",
    "test:parallel": "bun test --parallel",
    "typecheck": "tsc --noEmit",
    "port:fixtures": "bun run tools/port-fixtures.ts"
  },
  "devDependencies": { "typescript": "^5.9.0", "@types/bun": "latest" },
  "engines": { "bun": ">=1.4.0" }
}
