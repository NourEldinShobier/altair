{
  "name": "altair",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun test",
    "test:parallel": "bun test --parallel",
    "typecheck": "tsc --noEmit",
    "port:fixtures": "bun run tools/port-fixtures.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^7.0.2"
  },
  "engines": {
    "bun": ">=1.4.0"
  }
}
