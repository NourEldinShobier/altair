{
  "name": "altair",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "test": "bun test",
    "test:parallel": "bun test --parallel",
    "typecheck": "tsc --noEmit",
    "port:fixtures": "bun run tools/port-fixtures.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxfmt": "^0.64.0",
    "oxlint": "^1.79.0",
    "typescript": "^7.0.2"
  },
  "engines": {
    "bun": ">=1.4.0"
  }
}
