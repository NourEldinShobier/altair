### What does this change?

<!-- One paragraph. If it ports a Rails subsystem, name it. -->

### Rails parity

<!-- Which Rails source and tests does this mirror? Delete if not a port. -->

- Rails source:
- Rails tests mirrored:
- Ported test count:

### Checklist

- [ ] Tests pass (`bun test`)
- [ ] Typecheck passes (`bun run typecheck`)
- [ ] Tests mirror Rails' cases where Rails has them, with comments naming them
- [ ] `PARITY.md` updated
- [ ] Deliberate shortcuts marked with a `ponytail:` comment
